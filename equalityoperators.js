
/*
const age = 18;
if (age === 18) console.log('You just became an adult 😬');

if (age == 18) console.log('You just became an adult 😬');

//Equality operators will return a true or false value - boolean value. In this case, true will be the only result. This is because the constant is 18, so if the age is not equivalent to 18 the result will come back as false. ' ==== ' - strict equality operator, only returns true when both values are the same and has type coercion. '==' loose equality operator, there is no type coercion. so '18' == 18 will still come back as true.

// Always use strict operators.

const favourite = Number(prompt("What's your favourite number?"));
console.log(favourite);

if (favourite === 9) { // 9 === 9 // 21 === 9 -> FALSE
    console.log('Cool! 9 is an amazing number!');
} else if (favourite === 7) {
    console.log('7 is also a cool number!');
} else if (favourite === 23) {
    console.log('23 is one of the coolest numbers');
} else {
    console.log('Number is not 23 or 7 or 9');
}

if (favourite !== 23) console.log('Why not 23?');
*/
