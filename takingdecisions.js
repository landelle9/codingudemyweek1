/*const age = 15;
if (age >= 18) {
    console.log('Sarah can start her driving lessons 🚗');
}
else {
    const yearsLeft = 18 - age
    console.log(`Sarah is too young. Wait another ${yearsLeft} years :)`);
}

// The above if example shows that if the age equals to or is greater than 18, that the statement ' Sarah can start her driving lessons 🚗' will show on the console, through console.log.

// The else statement shows that if the age is lower that the variable is lower than the age of 18, that the statement 'Sarah is too young. Wait another x amount of years.

// Control structure = if + else statement

// if () {

// } else {

// }

const birthYear = 1997;

let century;
if (birthYear <= 2000) {
    century = 20;
} else {
    century = 21;
}
console.log(century);

*/
